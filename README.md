# interview-log-problem

```
usage: log_summary.py [-h] [-p PATH [PATH ...]] [-t THREADS] [-o OUT_DIR]

Summarize log files.

options:
  -h, --help            show this help message and exit
  -p, --path PATH [PATH ...]
                        Wildcard for log files
  -t, --threads THREADS
                        Number of threads to use
  -o, --out_dir OUT_DIR
                        Output directory

```
